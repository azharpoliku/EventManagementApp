import { defineConfig } from 'vite'

// Provisional repository name based on the current project folder.
// Update this value if the GitHub repository name differs.
export default defineConfig({
  base: '/EventManagementApp/',
})
